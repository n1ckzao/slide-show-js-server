{
  "name": "meu-json-server",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
    "start": "json-server --watch db.json --port $PORT --host 0.0.0.0"
  },
  "devDependencies": {
    "json-server": "^0.17.3"
  }
}
