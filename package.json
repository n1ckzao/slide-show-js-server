{
  "name": "meu-json-server",
  "version": "1.0.0",
  "main": "index.js",
  "type": "module",
  "scripts": {
  "start": "npx json-server --watch db.json --port 8080 --host 0.0.0.0"
},
  "devDependencies": {
    "json-server": "^1.0.0-beta.3"
  }
}
